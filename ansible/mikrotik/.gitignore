# Ignore backup configurations (may contain sensitive data)
configs/*.rsc
!configs/.gitkeep

# Secrets (passwords, API keys, etc.)
**/secrets.yml

# Claude workspace memory (may contain credentials)
CLAUDE.md
